apiVersion: 1

datasources:
  - name: influxdb-iot
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    editable: true

    jsonData:
      version: Flux
      organization: iot
      defaultBucket: iot

    secureJsonData:
      token: gas
